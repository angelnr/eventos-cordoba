-- Migration no-op: the schema.prisma already reflects the normalized place structure.
-- The Event model already has locationId and relation to Place.
-- The Place model already exists as a separate table.
-- This migration existed for the transition but is no longer needed on a fresh schema create.

